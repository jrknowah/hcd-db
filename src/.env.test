// ============================================================================
// .env.test - Test Environment Variables
// ============================================================================
/*
NODE_ENV=test
VITE_API_URL=http://localhost:5000
VITE_USE_MOCK_DATA=true

# Azure Test Configs (use separate test resources)
# AZURE_SQL_SERVER=test-server.database.windows.net
# AZURE_SQL_DATABASE=test-database
# AZURE_SQL_USER=testuser
# AZURE_SQL_PASSWORD=testpassword
# AZURE_STORAGE_CONNECTION_STRING=test-connection-string

# Disable real external services in tests
DISABLE_AZURE_AUTH=true

# Use mock data for tests (don't connect to real Azure)
USE_MOCK_DATA=true

# Disable real Azure connections
SKIP_AZURE_SQL=true
SKIP_AZURE_STORAGE=true

# Mock Azure SQL credentials (won't be used)
AZURE_SQL_SERVER=mock-server.database.windows.net
AZURE_SQL_DATABASE=mock-database
AZURE_SQL_USER=mockuser
AZURE_SQL_PASSWORD=mockpassword

# Mock Azure Storage
AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=mockaccount;AccountKey=bW9ja2tleQ==;EndpointSuffix=core.windows.net
AZURE_STORAGE_ACCOUNT=mockaccount
AZURE_CONTAINER_NAME=test-uploads

# API Configuration
# VITE_API_URL=http://localhost:5000
PORT=5000

*/